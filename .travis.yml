dist: trusty
language: cpp
compiler: gcc

addons:
  apt:
    packages:
    - cmake
    - lua5.2
    - libjson-c-dev
    - liblua5.2-dev
    
script:
- sudo find / -name "lua.h"
- cmake .
- make -j1
- make package
